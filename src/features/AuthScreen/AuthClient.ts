import { authUrl } from "@/src/shared/constants/endpoints";

export const login = () => {
  try {
    // TODO
  } catch (error) {
    console.error("login", error);
  }
};

export const register = () => {
  try {
    // TODO
  } catch (error) {
    console.error("register", error);
  }
};
